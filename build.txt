To build the nanim tools, you must have a working installation of:

- JDK
- Maven

Then run 'mvn clean install" in the root sdk directory.

After the build copy the generated **/target/nanim*.jar files in the bin directory
and read the installation instructions.
